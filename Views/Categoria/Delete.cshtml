@model mi_ferreteria.Models.Categoria
@{
    ViewData["Title"] = "Desactivar CategorA-a";
}
<h2>Desactivar CategorA-a</h2>
<div class="alert alert-warning">A�Seguro que deseas desactivar esta categorA-a? PodrA?s activarla nuevamente mA?s adelante.</div>
<form asp-action="Delete" method="post">
    <input type="hidden" name="id" value="@Model.Id" />
    <div class="mb-2">
        <label class="form-label">Nombre</label>
        <input class="form-control" value="@Model.Nombre" readonly />
    </div>
    <div class="d-flex justify-content-center gap-2 mt-3">
        <button type="submit" class="btn-danger">Desactivar</button>
        <a asp-action="Index" class="btn-secondary">Volver</a>
    </div>
</form>

